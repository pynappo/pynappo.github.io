---
const links = [
  ["/", "Home"],
  ["/about", "About"],
  ["/blog/", "Blog"],
  ["/tags/", "Tags"],
];
---

<div
  class="nav-links flex flex-row flex-nowrap space-x-2 content-center items-center"
>
  {
    links.map(([link, label]) => (
      <a href={link} class="hover:underline border-gray-500">
        {label}
      </a>
    ))
  }
</div>
